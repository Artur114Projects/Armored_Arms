plugins {
    id 'java'
}

version = coreVersion
group = 'com.artur114.armoredarms.core'
archivesBaseName = 'ArmoredArmsCore'

repositories {
    mavenCentral()
}

dependencies {
    compile 'it.unimi.dsi:fastutil:8.5.14'
    testImplementation 'org.junit.jupiter:junit-jupiter-api:5.9.1'
    testImplementation 'org.junit.jupiter:junit-jupiter-params:5.9.1'
    testRuntimeOnly 'org.junit.jupiter:junit-jupiter-engine:5.9.1'
}

tasks.withType(JavaCompile).configureEach {
    options.encoding = 'UTF-8'
}

jar {
    manifest {
        attributes([
            "Specification-Title": "Armored Arms Core",
            "Specification-Vendor": "armoredarmscore",
            "Specification-Version": coreVersion,
            "Implementation-Title": project.name,
            "Implementation-Version": "${version}",
            "Implementation-Vendor" :"armoredarmscore",
            "Implementation-Timestamp": new Date().format("yyyy-MM-dd'T'HH:mm:ssZ")
        ])
    }
}

test {
    useJUnitPlatform()
}